export class datos {
    public tarea: string;
    public descripcion: string;

    constructor(tarea: string, descripcion: string) {
        this.tarea = tarea;
        this.descripcion = descripcion;
    }
}